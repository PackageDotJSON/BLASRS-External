const DB_QUERIES = {
  FETCH_SUBMISSIONS:
    `SELECT COMPANY_NAME, UPLOAD_ID, SUBMITTED_WHEN, PERIOD_ENDED FROM CRS.BLASRS_DATA_SUBMITTER1 ORDER BY SUBMITTED_WHEN 
     DESC FETCH FIRST 100 ROWS ONLY`,
};

module.exports = DB_QUERIES;
